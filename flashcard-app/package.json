{
  "name": "flashcard-app",
  "version": "1.0.0",
  "description": "Flash Card React App",
  "main": "index.js",
  "scripts": {
    "build": "watchify src/app.js -o public/bundle.js -t [ babelify --presets [ react es2015 ] ]",
    "server": "cd public && live-server --port=1234 --entry-file=index.html"
  },
  "repository": {
    "type": "git",
    "url": "?.git"
  },
  "keywords": [
    "react"
  ],
  "author": "ngmanh",
  "license": "MIT",
  "dependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.3.0",
    "live-server": "^1.0.0",
    "watchify": "^3.7.0"
  }
}
